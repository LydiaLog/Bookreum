spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookreum?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root       # MySQL 사용자명
    password: 7h4saf0324!    # MySQL 비밀번호 (현재 설정된 비밀번호로 교체)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect   # MySQL 8.x 버전에 맞는 Dialect
    hibernate:
      ddl-auto: update # 테스트 시마다 테이블 자동 생성/삭제
    show-sql: true            # SQL 쿼리 로깅 (콘솔에서 확인 가능)
